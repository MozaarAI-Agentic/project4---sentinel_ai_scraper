# Shared development dependencies - not installed in production images.
# aiosqlite is here (not in any service's production requirements) because
# it's a test-only substitute for PostgreSQL, per ADR-0008.

pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
pytest-cov>=6.0,<7.0
aiosqlite>=0.20,<1.0
httpx>=0.27,<1.0
ruff>=0.7,<1.0
mypy>=1.13,<2.0
